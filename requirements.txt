# Requirements for Document Processing Agent (Simplified)
# Python 3.10+

# Core dependencies
python-dotenv==1.0.0

# LLM (локальная модель)
llama-cpp-python>=0.2.27  # GPU: CMAKE_ARGS="-DLLAMA_CUBLAS=on" pip install

# Document processing
docx2txt==0.8
openpyxl==3.1.2  # Excel файлы
pandas>=2.0.0

# Web interface
streamlit>=1.28.0

# PDF support (optional)
PyPDF2>=3.0.0

# Encryption for templates
cryptography>=41.0.0

# Email - используется встроенный imaplib

# ===================================
# Для GPU ускорения llama-cpp-python:
# pip uninstall llama-cpp-python
# CMAKE_ARGS="-DLLAMA_CUBLAS=on" pip install llama-cpp-python --no-cache-dir
# ===================================
mypy==1.7.1

# Jupyter (for analysis)
jupyter==1.0.0
ipykernel==6.27.1

# ===================================
# Installation notes
# ===================================

# For GPU support (CUDA 11.8+):
# 1. Install PyTorch with CUDA:
#    pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
#
# 2. Install llama-cpp-python with CUDA:
#    CMAKE_ARGS="-DLLAMA_CUBLAS=on" pip install llama-cpp-python --force-reinstall --no-cache-dir
#
# 3. Install FAISS with GPU:
#    pip install faiss-gpu
#
# For CPU only:
# Just use: pip install -r requirements.txt

# ===================================
# Additional notes
# ===================================

# Model download:
# Run: python models/download_model.py
# Or manually download from HuggingFace:
# - Llama-2-7B-Chat-GGUF (Q4_K_M): ~4 GB
# - Mistral-7B-Instruct-GGUF (Q4_K_M): ~4 GB

# Templates:
# Place contract templates in templates/ folder
# Supported formats: .docx, .txt
